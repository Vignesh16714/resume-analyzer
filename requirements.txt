# Core app
streamlit>=1.36.0
pandas>=2.2.0
plotly>=5.22.0

# Resume parsing
PyMuPDF>=1.24.0
python-docx>=1.1.0

# NLP / keyword extraction
spacy>=3.7.0

# LangChain (workflow orchestration hook — see README for how to extend
# the pipeline into a LangChain chain if desired)
langchain>=0.2.0

# HTTP client used by the "ollama" feedback backend
requests>=2.31.0

# --- Optional, heavy dependencies -----------------------------------
# Only required if you set FEEDBACK_BACKEND=huggingface in config.py / env.
# Left commented out so a fresh `pip install -r requirements.txt` stays
# fast and lightweight (default backend is "template", no ML downloads).
# transformers>=4.42.0
# torch>=2.3.0
